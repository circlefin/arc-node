# Release Flow Include 4

This fixture validates a follow-up patch release after the public test release
branch has already consumed the previous final handoff.

It is intentionally public-safe test content.
