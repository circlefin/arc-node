# Release Flow Include 3

This fixture validates a follow-up patch final release after the public test
release branch already contains the previous exported patch.

It is intentionally public-safe test content.
